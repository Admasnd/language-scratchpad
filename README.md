# Development Instructions

`devenv up -d` to start nginx
`process-compose attach` to connect to nginx process
`process-compose down` to stop nginx process
`sudo tailscale serve localhost:8080` to access web app over tailnet
